/**
 * Services Layer
 *
 * This directory contains the business logic of the application.
 * Services are called by controllers and interact with the data-access layer (Prisma).
 *
 * Example:
 *   export class UserService {
 *     async findById(id: string) { ... }
 *   }
 */
